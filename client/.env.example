# Optional. Leave empty in dev (Vite proxies /api to the Express server).
VITE_API_BASE=
# Used only by the wallet adapter connection. Balances are read through the backend.
VITE_SOLANA_RPC=https://api.mainnet-beta.solana.com
